<div class="container py-4">
  <h1 class="text-center text-primary mb-4">Quản lý Khách hàng</h1>

  <!-- Form thêm/sửa khách hàng -->
  <div class="card shadow-sm mb-4">
    <div class="card-body">
      <h5 class="card-title">Thông tin khách hàng</h5>
      <div class="row g-3">
        <input type="hidden" id="kh_id">

        <div class="col-md-4">
          <input type="text" id="kh_name" class="form-control" placeholder="Tên khách hàng">
        </div>
        <div class="col-md-4">
          <input type="text" id="kh_phone" class="form-control" placeholder="Số điện thoại">
        </div>
        <div class="col-md-4">
          <input type="text" id="kh_address" class="form-control" placeholder="Địa chỉ">
        </div>

        <div class="col-md-12">
          <button id="addKH" class="btn btn-success">➕ Thêm</button>
          <button id="updateKH" class="btn btn-warning d-none">✏️ Cập nhật</button>
          <button id="cancelEditKH" class="btn btn-secondary d-none">❌ Huỷ</button>
        </div>
      </div>
    </div>
  </div>

  <!-- Bảng khách hàng -->
  <div class="card shadow-sm">
    <div class="card-body">
      <h5 class="card-title">Danh sách khách hàng</h5>
      <div class="table-responsive">
        <table class="table table-striped table-bordered align-middle text-center">
          <thead class="table-primary">
            <tr>
              <th>Tên</th>
              <th>Số điện thoại</th>
              <th>Địa chỉ</th>
              <th>Hành động</th>
            </tr>
          </thead>
          <tbody id="customerTable">
            {{#each khachhangs}}
            <tr data-id="{{this._id}}">
              <td>{{this.name}}</td>
              <td>{{this.phone}}</td>
              <td>{{this.address}}</td>
              <td>
                <button class="btn btn-sm btn-warning editKH" data-id="{{this._id}}" data-name="{{this.name}}"
                  data-phone="{{this.phone}}" data-address="{{this.address}}">✏️ Sửa</button>
                <button class="btn btn-sm btn-danger deleteKH" data-id="{{this._id}}">🗑️ Xoá</button>
              </td>
            </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>